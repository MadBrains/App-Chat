import { styled } from '@mui/material/styles';

export const SearchBlockContainer = styled('div')({
  display: 'flex',
  flexDirection: 'column'
});

export const SearchBlockHead = styled('div')({
  width: '100%',
  display: 'flex',
  marginTop: '57px'
});

export const BlockUsersBlock = styled('div')({
  marginTop: '15px'
});
